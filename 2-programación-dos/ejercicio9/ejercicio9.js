'use strict';

const text = document.querySelector('h1');

const name = "Gisell Mazo Rúa";

const textContent = `El nombre de mi compañera es ${name} y esta compuesto por ` + name.length + ` caracteres.`

text.innerHTML = textContent;


